/* import {Swiper, SwiperSlide} from 'swiper/react';
import { Navigation, Mousewheel, Keyboard } from "swiper";

import { FoodCard } from '../FoodCard';

export function SwiperSlider({ children, settings }) {

  return ( 
      <Swiper
        spaceBetween={50}
        slidesPerView={3}
        navigation={true}
        keyboard={{
          enabled: true,
        }}
        mousewheel={true}
        modules={[Navigation, Mousewheel, Keyboard]}
      >
        <SwiperSlide>
          {children}
        </SwiperSlide>
      </Swiper>

  );
} */