import { SelectRoot } from './SelectRoot';
import { SelectLabel } from './SelectLabel';
import { SelectWrapper } from './SelectWrapper';
import { SelectInput } from './SelectInput';
import { SelectInner } from './SelectInner';
import { SelectButton } from './SelectButton';

export const Select = {
  Root: SelectRoot,
  Label: SelectLabel,
  Wrapper: SelectWrapper,
  Input: SelectInput,
  Inner: SelectInner,
  Button: SelectButton,
}

/* import { Container, SelectContainer, InnerSelect, ChangeButton, OptionsList } from './styles';
 
import { useState } from 'react';

import { Button } from '../Button';

//import { ReactComponent as DropDown } from '../../assets/icons/CaretDown.svg';
//import { ReactComponent as NewOption } from '../../assets/icons/Plus.svg';

export function Select({ label, id, name, value, onChange, options, ...rest }) {
  
  const [isOpen, setIsOpen] = useState(false); */

/*   function handleToggleOpen() {
    setIsOpen(prevState => !prevState)
  } */

/*   return (
    <Container 
      {...rest}
    > */
{/*       { label && 
        
        <label htmlFor={id}>
          {label}
        </label>
      } */}
{/*       <SelectContainer
        className='select'
      >   */}
{/*         <InnerSelect
          onClick={handleToggleOpen}
        >
          <span 
            id={id}
          >
            {value?.label}
          </span>
          <Button
            type='button' 
            icon={DropDown}
          />
        </InnerSelect>
 */}
        {/* <input 
          type='text' 
          placeholder='Nova categoria'
        /> */}
        
{/*         <ChangeButton>
          <Button 
            type='button'
            icon={NewOption}
          />
        </ChangeButton> */}
{/*         <OptionsList className={isOpen ? 'show' : ''} >
          {options.map((option) => (
            
            <li
              key={option.label}
            >
              {option.label}
            </li>

          ))}
        </OptionsList> */}
      {/* </SelectContainer>  */}
    {/* </Container> 
  )
} */}